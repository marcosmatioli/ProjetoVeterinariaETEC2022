@model ProjetoVeterinaria.Models.modelCliente

@{
    ViewBag.Title = "Pagina Inicial";
}
<h2>@ViewBag.Title</h2>
<h3>@ViewBag.Message</h3>

@using (Html.BeginForm("Index", "Home", FormMethod.Post))
    {
        <fieldset>
            <!--essa view bag com Message ela recebe a mensagem caso o espertinho tente acessar a pagina sem logar OU
        tenta entrar na pagina de ADM sem estar com as credencias de ADM, então para evitar ele volta para a
        pagina inicial , então ele vai ser uma mensagem importante para o processo de login-->
            <br />
            <h2>Entrar</h2>
            <br />
            <div class="form-group">
                <div class="input-group">
                    <span class="input-group-addon"><i style="font-size:20px;" class="fas fa-envelope"></i></span>
                    @Html.EditorFor(x => x.usuario, new
               {
                   htmlAttributes = new { id = "txtUsuario", senha = "txtUsuario", type = "text", @class = "form-control", placeholder = "Digite o usuario", required = "required", maxlength = "30" }
               })
                    @Html.ValidationMessageFor(x => x.usuario, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                <div class="input-group">
                    <span class="input-group-addon"><i style="font-size:20px;" class="fas fa-lock"></i></span>
                    @Html.EditorFor(x => x.senha, new
               {
                   htmlAttributes = new { id = "txtSenha", senha = "txtSenha", type = "password", @class = "form-control", placeholder = "Digite sua senha", required = "required", maxlength = "10" }
               })
                    @Html.ValidationMessageFor(x => x.senha, "", new { @class = "text-danger" })
                </div>
            </div>

            <input type="submit" class="button button-a button-big button-rouded" value="ENTRAR"><br />
            <br />
            <p>@Html.ActionLink("Novo por aqui? Cadastre-se agora!", "cadCliente")</p>
        </fieldset>
}
